import {Typography} from 'antd'
const {Text} = Typography

const High = () => (
    <Text strong style={{ color: "#009acc", cursor: "default", textShadow: "0 0 1em  #009accaa" }}>HIGH</Text>
)

export default High;