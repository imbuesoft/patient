import React from 'react';
import PathologyForm from './pages/PathologyForm';

const index = (props) => {
    return (
        <>
            <PathologyForm {...props} />
        </>
    );
};

export default index;