import { useEffect } from 'react';
import SearchBox from './pages/SearchBox';

const index = (props) => {

    return (
        <>
            <SearchBox {...props} />
        </>
    );
};

export default index;